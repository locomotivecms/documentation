{% extends 'reference/filters' %}

{% block 'content' %}


:markdown
  # underscore(input)

  Makes an underscored, lowercase form from the expression in the string.

  {% raw %}
      {{ 'FooBar' | underscore }}
  {% endraw %}

  **Example**: Returns foo_bar

{% endblock %}