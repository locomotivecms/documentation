---
title: If/Else/Unless
published: true
listed: true
position: 1
---
{% extends 'references/tags' %}

{% block 'main/left/content' %}

:markdown
  # if / else / unless

  Boolean logic operations are available on all objects.

      {% raw %}{% if feature.title %}
        {{ feature.title }}
      {% endif %}
      {% unless feature.body %}
        feature has no content
      {% else %}
        {{ feature.body }}
      {% endunless %}
  {% endraw %}

{% endblock %}
