%nav.subnav
  %ul
    {% assign path_segments = path | split: '/' %}
    {% for child in page.parent.parent.parent.children %}
    %li
      %a{ :href => "/{{ child.fullpath }}", :class => "{% if child.slug == path_segments[3] %}current{% endif %}" }
        {{ child.title }}
    {% endfor %}